"use client";
import { useEffect } from "react";
export default function HomeInteractions(){useEffect(()=>{const root=document.querySelector<HTMLElement>(".nr2");if(!root)return;const reduce=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const nav=root.querySelector<HTMLElement>(".nr2-nav");const stage=root.querySelector<HTMLElement>(".nr2-stage");const consoleEl=root.querySelector<HTMLElement>(".nr2-console");const targets=Array.from(root.querySelectorAll<HTMLElement>(".nr2-section,.nr2-console,.nr2-cta"));if(reduce){targets.forEach(el=>el.classList.add("is-visible"));return}
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:"0px 0px -8% 0px"});targets.forEach(el=>{el.classList.add("nr2-reveal");observer.observe(el)});
let raf=0;const onMove=(event:PointerEvent)=>{if(!stage||!consoleEl||event.pointerType==="touch")return;const rect=stage.getBoundingClientRect();const x=(event.clientX-rect.left)/rect.width-.5;const y=(event.clientY-rect.top)/rect.height-.5;cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{consoleEl.style.setProperty("--tilt-x",`${x*3.5}deg`);consoleEl.style.setProperty("--tilt-y",`${y*-2.2}deg`)})};const reset=()=>{if(!consoleEl)return;consoleEl.style.removeProperty("--tilt-x");consoleEl.style.removeProperty("--tilt-y")};stage?.addEventListener("pointermove",onMove);stage?.addEventListener("pointerleave",reset);const onScroll=()=>{nav?.toggleAttribute("data-scrolled",window.scrollY>12)};onScroll();window.addEventListener("scroll",onScroll,{passive:true});return()=>{observer.disconnect();cancelAnimationFrame(raf);stage?.removeEventListener("pointermove",onMove);stage?.removeEventListener("pointerleave",reset);window.removeEventListener("scroll",onScroll)}},[]);return null}
